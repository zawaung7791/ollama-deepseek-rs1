{
    "image":"mcr.microsoft.com/devcontainers/universal:2",
    "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && curl -sSL https://ollama.com/install.sh | bash"
}